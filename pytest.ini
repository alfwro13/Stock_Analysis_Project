[pytest]
pythonpath = .
testpaths = tests
markers =
    db:     Database schema and integrity tests
    api:    API endpoint tests (GET and POST)
    pages:  HTML page route tests
    math:   Pure computation tests (indicators, position sizing, risk)
    config: Configuration and utility tests
addopts = --tb=short

